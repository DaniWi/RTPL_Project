project Mathgame is

   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Main use ("main.adb");

   package Compiler is
      for Switches ("ada") use ("-g", "-gnatyyy", "-gnatwa.n", "-gnatf", "-gnato", "-gnata", "-gnatVa");
   end Compiler;

end Mathgame;

